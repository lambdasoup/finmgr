syntax = "proto3";

package finmgr;

message AccountInfo {
  string id = 1;
  string pin = 2;
  string blz = 3;
}

message Accounts {
  string info = 1;
}

service Service {
  rpc GetAccounts(AccountInfo) returns (Accounts) {}
}
